{
        "run_list" : [
                "recipe[apt]"
        ],
        "ganglia": {
        	"cluster_name": "CHOReOS",
        	"unicast": true,
        	"server_addresses": [ "localhost" ]
		}
}
